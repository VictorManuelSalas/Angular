//Esto sirve pára determinar que componentes o elementos con sus tipos de datos
//debe de tener el array al cual se le agregara esta interface


//Si no tiene el export no se podra exportar
export interface Product {
    name: string;
    price: number;
    image: string;
}